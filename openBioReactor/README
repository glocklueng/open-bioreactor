*-----------------------------------------------------------------*
The files in 'Bioreactor_pde' is only compatible with "Arduino 0022"

The 'libraries' have to be in the root directory of the Arduino IDE

*-----------------------------------------------------------------*

The files in 'Bioreactor_ino' is only compatible with "Arduino-1.0-RC1".

If you're using the release candidate of Arduino IDE "Arduino-1.0-RC1", you have to use the 'libraries_ino' (some libraries had to be modified slightly).

NOTE: The project is till being ported to the new Arduino IDE.

*-----------------------------------------------------------------*
